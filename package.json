{
  "name": "html-to-pdf",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "This project is a Node.js script that uses Puppeteer to convert a webpage to a PDF file. The generated PDF is stored in an outputs directory with a filename based on the base URL of the website and a timestamp to ensure uniqueness. ",
  "dependencies": {
    "puppeteer-core": "^22.14.0"
  }
}
